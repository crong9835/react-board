# react-board 프로젝트 정리

React + Vite + Supabase로 만든 게시판입니다. 이 문서는 **무엇을 만들었고, 왜 그렇게 만들었는지**를 한곳에 모은 정리본입니다.

- 배포 주소: https://react-board-psi.vercel.app/
- 저장소 기준 브랜치: `main`

---

## 1. 한눈에 보기

| 구분 | 내용 |
|---|---|
| 무엇 | 회원가입·로그인 후 글을 쓰고, 본인 글만 수정·삭제하는 게시판 |
| 프론트엔드 | React 19, React Router 7 (컴포넌트 라우팅 방식), Vite 8 |
| 백엔드 | Supabase — Database(PostgreSQL) + Auth + RLS |
| 배포 | Vercel |
| 서버 코드 | 없음. 브라우저가 Supabase에 직접 요청하고, 권한 판단은 DB의 RLS가 담당 |

서버를 따로 두지 않았기 때문에 **"브라우저 코드는 사용자가 고칠 수 있다"** 는 전제가 이 프로젝트 설계의 중심입니다. 화면에서 하는 검사는 안내용이고, 실제 차단은 항상 DB에서 합니다.

---

## 2. 화면과 라우트

| 주소 | 화면 | 접근 조건 |
|---|---|---|
| `/` | 글 목록 (15개씩 페이지네이션 + 제목·작성자 검색) | 누구나 |
| `/post/:id` | 글 상세 (+ 본인 글이면 수정·삭제 버튼) | 누구나 |
| `/write` | 글쓰기 | 로그인 필요 (아니면 `/login`) |
| `/edit/:id` | 글 수정 | 로그인 + 본인 글 (아니면 상세로 되돌림) |
| `/login`, `/signup` | 로그인 / 회원가입 | 누구나 |
| `/soon/:name` | 아직 안 만든 메뉴 안내 | 누구나 |
| 그 외 전부 | 404 화면 | — |

목록의 페이지 번호와 검색 조건은 `useState`가 아니라 **주소(`?page=2&type=title&q=고양이`)** 에 둡니다. 새로고침·뒤로가기·링크 공유에서 같은 화면이 유지되기 때문입니다.

상세로 넘어갈 때는 **주소를 통째로** 실어 보냅니다. 예전에는 `?page=${page}`처럼 페이지 번호만 적었는데, 그때는 그것뿐이라 문제가 없었지만 검색 조건이 생기면서 조건이 늘 때마다 링크를 만드는 자리를 전부 고쳐야 하는 구조였습니다. 통째로 넘기면 나중에 무엇이 늘어도 그 코드는 그대로입니다. 이 주소는 `상세 → 수정 → 상세 → 목록`까지 따라다닙니다.

---

## 3. 데이터 구조

### posts — 글

앱이 사용하는 컬럼입니다.

| 컬럼 | 용도 |
|---|---|
| `id` | 글 번호 (최신순 정렬과 주소 `/post/:id`에 사용) |
| `title`, `content` | 제목 30자, 본문 500자 제한 |
| `writer` | **닉네임**을 저장합니다. 이메일이 아닙니다 (7-1 참고) |
| `user_id` | 작성자 계정 id. "본인 글"을 판단하는 근거 |
| `created_at` | 작성일 |

### comments — 댓글 (`supabase/comments.sql`)

| 컬럼 | 용도 |
|---|---|
| `id` | 댓글 번호 (정렬 기준) |
| `post_id` | 어느 글의 댓글인지. `posts.id`가 **bigint**라 여기도 bigint |
| `user_id` | 작성자 계정 id. "본인 댓글"을 판단하는 근거 |
| `writer` | 닉네임. `posts.writer`와 같은 이유로 이메일을 넣지 않습니다 |
| `content` | 200자 제한 |
| `created_at` | 작성일 |

**`on delete cascade`가 이 표의 핵심입니다.** `post_id`에 걸어두면 글이 지워질 때 그 글의 댓글도 함께 사라집니다. 이게 없으면 댓글이 달린 글을 지울 때 "이 글을 가리키는 댓글이 아직 남아 있다"며 DB가 거부해서, 화면에서 삭제 버튼을 눌러도 삭제가 실패합니다. `user_id`에도 걸어 계정이 지워지면 그 사람의 댓글도 사라지게 했습니다.

> `posts.id`가 bigint인지는 Supabase 대시보드 없이도 확인할 수 있었습니다. int4 범위를 넘는 값(`id=eq.2147483648`)으로 조회했을 때 "범위 초과" 에러가 아니라 빈 결과가 오면 bigint입니다.

### profiles — 닉네임 (`supabase/profiles.sql`)

Supabase가 계정을 보관하는 `auth.users`에는 이메일이 들어 있어 브라우저에서 읽을 수 없습니다. 그래서 **화면에 보여도 되는 정보만** 담는 표를 따로 두었습니다.

- `id`(= `auth.users.id`), `nickname`, `created_at`
- `lower(nickname)`에 unique 인덱스 → `crong`과 `Crong`을 같은 닉네임으로 보고 막습니다
- 조회 정책만 열고 **등록·수정·삭제 정책은 만들지 않았습니다.** 정책이 없으면 아무도 못 합니다. 닉네임 줄은 가입 순간 트리거가 딱 한 번 만들고, 이후 변경 불가입니다

가입 시 닉네임을 브라우저가 직접 넣지 않고 DB 트리거(`handle_new_user`, `security definer`)로 만드는 이유: 나중에 이메일 인증을 켜면 메일 확인 전까지 로그인 상태가 아니라 브라우저가 이 표에 값을 넣을 수 없습니다. 트리거는 DB 안에서 도는 일이라 인증 설정과 무관하게 동작합니다.

---

## 4. 데이터를 가져오는 구조 ★

이 프로젝트에서 구조를 한 번 크게 바꾼 부분입니다.

### 지금 방식 — 각 페이지가 필요한 만큼만 직접 조회

```
App.jsx        글 데이터를 들고 있지 않습니다 (라우팅 + 헤더만)
 ├─ PostList    그 페이지 15줄, 목록에 쓰는 컬럼만
 ├─ PostDetail  그 글 하나, 전 컬럼(본문 포함)
 └─ PostEdit    그 글 하나, 전 컬럼
```

**목록 — `src/pages/PostList.jsx`**

```jsx
const from = (page - 1) * PAGE_SIZE;   // 앞에서 건너뛸 개수 = offset
const to = from + PAGE_SIZE - 1;       // range 는 끝 번호를 포함하므로 1을 뺌

const { data, count, error } = await supabase
  .from('posts')
  .select('id, title, writer, created_at', { count: 'exact' })
  .order('id', { ascending: false })
  .range(from, to);
```

- 목록은 본문(`content`)을 한 글자도 쓰지 않는데 가장 긴 컬럼입니다. 컬럼을 지정해 빼는 것만으로 받아오는 양이 크게 줄어듭니다
- 전체 글을 받지 않으니 `posts.length`로 총 개수를 셀 수 없어, `count: 'exact'`로 DB에 따로 물어 페이지 버튼을 계산합니다
- **`- 1`이 핵심입니다.** `slice(15, 30)`은 30번을 포함하지 않지만 `range(15, 30)`은 포함해 16개를 줍니다

**상세·수정 — `PostDetail.jsx`, `PostEdit.jsx`**

```jsx
const { data, error } = await supabase
  .from('posts').select('*').eq('id', postId).maybeSingle();
```

`single()`은 행이 없으면 에러로 보지만, `maybeSingle()`은 `data`를 `null`로 줍니다. 지워진 글 주소로 들어온 것은 에러가 아니라 "없는 글"이므로 `maybeSingle()`이 맞습니다. `/post/abc`처럼 숫자가 아닌 주소는 `Number.isInteger`로 먼저 걸러 조회 자체를 하지 않습니다.

### 이전 방식과 왜 바꿨는지

이전에는 `App.jsx`가 `select('*')`로 **전체 글을 한 번에** 받아 목록·상세·수정 세 페이지에 props로 넘겼습니다. 페이지 나누기도 받아온 배열을 `slice`로 자르는 클라이언트 페이징이었습니다.

| | 이전 | 지금 |
|---|---|---|
| 목록 요청 | 전체 글, 본문 포함 | 15줄, 본문 제외 |
| 총 개수 | `posts.length` | DB `count` |
| 페이지 나누기 | 브라우저 `slice` | 서버 `range()` |
| 상세·수정 | 배열에서 `find` | `id`로 한 건 조회 |
| 글 추가·수정·삭제 후 | `setPosts`로 배열 조작 | 화면이 열릴 때 다시 읽음 |

이전 방식의 장점도 분명했습니다. 상태가 한 곳(`App`)에만 있어 단순하고, 상세·수정 페이지가 추가 요청 없이 즉시 열렸습니다. 문제는 **글 개수에 비례해 첫 로딩이 계속 무거워진다**는 점이었습니다. 글이 1000개가 되면 15개만 보려는 사람도 본문 1000개를 다운로드합니다.

바꾸면서 오히려 단순해진 것도 있습니다. 등록·수정·삭제 후 `setPosts`로 배열을 손질해 화면을 DB와 맞추던 코드가 사라졌습니다. 화면이 열릴 때 DB를 다시 읽으므로 어긋날 여지가 없습니다.

### 알아둘 offset 방식의 한계

1. **offset이 커지면 느려집니다.** DB는 "5만 개 건너뛰기"를 건너뛸 5만 개를 실제로 읽고 버리는 방식으로 처리합니다. 뒤쪽 페이지일수록 느려집니다
2. **페이지를 넘기는 사이 글이 추가되면 한 칸씩 밀립니다.** 1페이지 마지막 글이 2페이지 첫 줄에 다시 보일 수 있습니다

2번을 없애려면 "마지막으로 본 id보다 작은 것 15개"로 묻는 커서 페이징이 필요하지만, 그러면 페이지 번호로 점프할 수 없습니다. **1·2·3 페이지 버튼이 있는 게시판에는 offset 방식이 맞습니다.** 커서 방식은 "더 보기"나 무한 스크롤에 적합합니다.

### 댓글도 나눠서 받습니다 (`src/components/Comments.jsx`) ★

댓글도 같은 함정이 있습니다. 처음에는 그 글의 댓글을 **전부** 받아왔는데, 이는 위에서 고친 "전체 글을 한 번에 받아오던 구조"를 댓글에서 그대로 반복한 것이었습니다. 20개씩 나눠 받고 "이전 댓글 더 보기"로 늘리도록 바꿨습니다.

**DB에는 최신 것부터 달라고 하고, 화면에 그릴 때 뒤집습니다.**

```js
const { data, count } = await supabase
  .from('comments')
  .select('id, user_id, writer, content, created_at', { count: 'exact' })
  .eq('post_id', postId)
  .order('id', { ascending: false })   // 최신 것부터
  .range(0, loadedCount - 1);

setComments([...data].reverse());      // 화면에는 오래된 순으로
```

화면에는 오래된 댓글이 위에 있어야 합니다. 위에서 아래로 읽어 내려가는 대화에 가깝기 때문입니다. 하지만 **나눠 받는 기준은 최신 쪽이어야 합니다.** 오래된 것부터 20개를 받으면, 댓글을 새로 써도 그것은 맨 뒤에 있어서 화면에 나타나지 않습니다. 자기가 쓴 댓글이 안 보이는 것만큼 이상한 일이 없습니다. 최신 20개를 받으면 방금 쓴 댓글은 항상 그 안에 들어 있습니다.

`loadedCount`를 20 늘리면 `useEffect`가 다시 돌아 그만큼 받아옵니다. 이미 본 댓글까지 다시 받는 셈이지만, 배열을 이어붙이는 방식보다 코드가 단순하고 등록·삭제 후 처리가 저절로 맞습니다.

> 글 목록에서 겪은 offset의 한계 2번("페이지를 넘기는 사이 글이 추가되면 한 칸씩 밀린다")은 목록이 **최신순**이라 새 글이 맨 앞에 끼어들기 때문이었습니다. 댓글은 화면상 오래된 순이고 새 댓글은 항상 맨 뒤에 붙으므로, 읽고 있던 부분이 밀리지 않습니다.

### 목록 검색 (`src/search.js`)

제목 또는 작성자를 고르고 검색어를 넣으면 `?type=title&q=고양이`가 주소에 붙습니다. 조회는 조건을 하나 더 얹는 것이 전부입니다.

```js
let query = supabase
  .from('posts')
  .select('id, title, writer, created_at', { count: 'exact' })
  .order('id', { ascending: false });

query = applySearch(query, searchType, keyword);   // 검색어가 없으면 그대로 통과

const { data, count, error } = await query.range(from, to);
```

**페이지 계산 코드는 한 줄도 고치지 않았습니다.** `count: 'exact'`가 "조건에 맞는" 개수를 세주기 때문에, 검색 중이면 검색에 걸린 개수가 그대로 들어와 페이지 버튼이 알아서 맞습니다.

**제목과 작성자를 `or()`로 한 번에 찾지 않았습니다.** `or()`는 조건을 `'title.ilike.%가%,writer.ilike.%가%'`처럼 한 줄의 글자로 적는데, 여기서 쉼표와 괄호가 조건을 나누는 문법입니다. 검색어에 쉼표나 괄호가 들어가면 조건이 엉뚱하게 쪼개집니다. 한 번에 한 컬럼만 보면 그 위험이 없습니다.

#### 검색어의 와일드카드 (실제로 걸렸던 문제) ★

`ilike`에서 아래 세 글자는 특별한 뜻을 가집니다.

| 글자 | 뜻 |
|---|---|
| `%` | 아무 글자가 몇 개든 |
| `_` | 아무 글자 한 개 |
| `*` | Supabase(PostgREST) 서버가 `%`로 바꿔서 처리 |

그래서 검색어에 `%` 한 글자만 넣어도 조건이 `%%%`가 되어 **전체 글이 걸립니다.** 실제로 확인해 보니 글이 207개일 때 `%` / `_` / `*` 셋 다 207개를 그대로 돌려줬습니다.

이 프로젝트는 회원가입의 닉네임 중복 검사에서 이미 같은 문제를 겪었습니다(7절). 그때는 닉네임에 특수문자를 **못 쓰게 막아서** 피했지만, 검색어는 무엇이든 칠 수 있어야 하므로 막는 대신 글자 그대로 찾도록 바꿉니다.

```js
export function escapeKeyword(keyword) {
  return keyword
    .replace(/\\/g, '\\\\')   // 백슬래시를 먼저
    .replace(/%/g, '\\%')
    .replace(/_/g, '\\_')
    .replace(/\*/g, '\\*');
}
```

`\%`는 "특별한 뜻 없는 `%` 글자"라는 표시입니다. **순서가 중요합니다.** `%`를 먼저 `\%`로 바꾸고 나서 백슬래시를 `\\`로 바꾸면, 방금 붙인 백슬래시까지 덩달아 늘어나 `\\%`가 되어 버립니다.

`*`는 제거하지 않고 이스케이프합니다. 제거하면 `*` 한 글자만 검색했을 때 검색어가 통째로 사라져 빈 검색이 되고, 결국 전체 글이 나와 원래 문제로 되돌아갑니다.

---

## 5. 로그인 상태 관리 (`src/AuthContext.jsx`)

```jsx
supabase.auth.getSession().then(({ data }) => {      // 1) 켜질 때 현재 세션 확인
  setUser(data.session ? data.session.user : null);
  setLoading(false);
});
const { data: listener } = supabase.auth.onAuthStateChange(  // 2) 이후 자동 갱신
  (_event, session) => setUser(session ? session.user : null)
);
return () => listener.subscription.unsubscribe();
```

신경 쓴 지점 세 가지입니다.

- **`loading`으로 "로그인 안 함"과 "아직 모름"을 구분합니다.** `user === null` 하나로는 두 상태가 섞여서, 앱이 켜지는 순간 보호 라우트가 "로그인 안 함"으로 오판정해 `/write`에서 로그인 페이지로 튕깁니다
- **닉네임 조회는 `user` 객체 전체가 아니라 `userId`만 감시합니다.** Supabase가 세션을 주기적으로 갱신하며 내용이 같은 새 객체를 만들기 때문에, `user`를 감시하면 그때마다 불필요하게 다시 조회합니다
- **경쟁 상태를 `isOutdated` 플래그로 막습니다.** 닉네임을 가져오는 중에 로그아웃하거나 계정을 바꾸면 뒤늦게 도착한 옛 응답이 새 값을 덮어씁니다

---

## 6. 권한 — 세 겹으로 막습니다

### 6-1. DB의 RLS 정책 (실제 차단, `supabase/policy.sql`)

```sql
alter table posts enable row level security;

create policy "누구나 글을 읽을 수 있음" on posts for select to public using (true);
create policy "본인 글만 수정"       on posts for update to public using (auth.uid() = user_id);
create policy "본인 글만 삭제"       on posts for delete to public using (auth.uid() = user_id);

create policy "본인 명의로, 시간당 10개까지 글 작성"
on posts for insert to authenticated
with check (
  (auth.uid() = user_id)
  and ((select count(*) from posts posts_1
        where posts_1.user_id = auth.uid()
          and posts_1.created_at > (now() - interval '1 hour')) < 10)
);
```

- `using`은 **이미 있는 행**(조회·수정·삭제), `with check`는 **새로 들어오거나 바뀐 행**(등록·수정)을 판단합니다
- 수정 정책에 `with check`를 따로 적지 않으면 `using` 조건이 바뀐 뒤의 행에도 적용됩니다. 덕분에 남의 글 수정뿐 아니라 **내 글의 `user_id`를 남의 id로 바꿔치기하는 것까지** 막힙니다
- 등록은 본인 명의 + 최근 1시간에 10개 미만일 때만 통과합니다. 걸리면 Supabase가 `42501`로 응답합니다

`comments`도 같은 구조입니다. 조회는 누구나, 등록은 본인 명의 + 시간당 30개까지, 삭제는 본인 댓글만. **수정 정책은 일부러 만들지 않았습니다.** 정책이 없으면 아무도 못 하기 때문에, 화면에 수정 버튼을 두지 않는 것에 더해 DB에서도 길이 막힙니다. `profiles`에서 닉네임을 바꿀 수 없게 한 것과 같은 방식입니다.

### 6-2. 처리된 행 수 확인 (오안내 방지)

```jsx
const { data, error } = await supabase
  .from('posts').update({ title, content })
  .eq('id', post.id).eq('user_id', user.id)
  .select();                 // 없으면 204라 몇 건 처리됐는지 알 수 없음

if (data.length === 0) { openModal('본인이 작성한 글만 수정할 수 있습니다.'); return; }
```

**RLS가 막은 UPDATE/DELETE는 에러가 아니라 "0건 처리"로 옵니다.** `error`만 보면 `null`이라 실패를 "수정되었습니다"로 잘못 안내합니다. `.select()`를 붙여 돌아온 행 수를 세는 것이 이 문제의 해법입니다. 삭제도 `PostDetail.jsx`에서 같은 패턴을 씁니다.

`.eq('user_id', user.id)`는 RLS와 겹치는 조건이지만, "내 글만 건드린다"는 의도를 코드에도 드러내려고 함께 적었습니다.

### 6-3. 화면 진입 차단 (UX)

```jsx
if (!user || post.user_id !== user.id) {
  return <Navigate to={detailPath} replace />;
}
```

주소창에 `/edit/3`을 직접 쳐서 남의 글 수정 폼에 들어오는 경우입니다. 실제 차단은 RLS가 하므로 이것은 보안이 아니라 **어차피 저장 못 할 폼을 보여주지 않는** 처리입니다.

---

## 7. 입력값 검증

| 대상 | 검사 |
|---|---|
| 제목·본문 | `trim()`으로 공백만 입력한 경우까지 걸러냄. `maxLength` 30 / 500 + 실시간 카운터 |
| 이메일 | `/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/`. 브라우저 `type="email"`은 도메인에 점이 있는지 보지 않아 `haha@haha`도 통과시킵니다 |
| 비밀번호 | 6자 이상 (Supabase 기본값. 서버도 막지만 먼저 알려주는 편이 빠름) |
| 닉네임 | 한글·영문·숫자 2~10자. 특수문자를 막는 이유가 하나 더 있는데, 중복 검사에 쓰는 `ilike`가 `%`와 `_`를 "아무 글자"로 해석해 `%%` 같은 닉네임이 남의 닉네임까지 걸러버립니다 |
| 닉네임 중복 | `ilike`로 대소문자 무시 조회. 두 사람이 동시에 가입하면 놓칠 수 있어 **최종 차단은 DB의 unique 인덱스** |
| 중복 제출 | 등록·수정·삭제·로그인·회원가입 모두 `isSubmitting` 등으로 버튼을 잠금 |

회원가입에는 한 가지 판별이 더 있습니다. **이미 가입된 이메일인지** 확인하는 부분입니다. "이 이메일은 이미 가입돼 있습니다"를 그대로 알려주면 이메일을 하나씩 넣어보며 가입자를 알아낼 수 있으므로, Supabase는 이메일 인증이 켜져 있을 때 성공한 것처럼 응답합니다. 다만 그때는 `data.user.identities`가 빈 배열로 오기 때문에, 이 차이로 구분해 "가입 확인 메일을 보냈습니다"라고 잘못 안내하는 것을 막았습니다.

---

## 8. 사용자 경험 세부 처리

- **없는 페이지 번호는 404.** `?page=999`, `?page=abc`, `?page=0`, `?page=2.7` 모두 목록이 아니라 404 화면입니다. 몰래 1페이지로 보내면 주소와 화면이 서로 다른 말을 하게 됩니다
- **마지막 페이지 높이 고정.** 마지막 페이지는 글이 15개보다 적을 수 있는데, 그대로 두면 페이지를 옮길 때마다 목록 높이가 줄어 화면이 출렁입니다. 모자란 만큼 빈 줄로 채웁니다
- **페이지 이동 중에는 로딩 문구로 갈아치우지 않습니다.** 목록이 사라지면 같은 이유로 높이가 출렁이므로, 첫 진입에만 "불러오는 중"을 띄우고 이후에는 새 글이 도착할 때 한 번에 바꿉니다
- **삭제 후 사라진 페이지로 돌아가지 않습니다.** 글 16개(2페이지)에서 2페이지의 마지막 글을 지우면 1페이지만 남는데, `?page=2`로 돌아가면 404가 뜹니다. 남은 개수를 DB에 물어 마지막 페이지를 다시 계산해 맞춥니다
- **"검색 결과가 없습니다"와 "등록된 글이 없습니다"를 구분합니다.** 검색 결과가 없는 것을 후자로 안내하면, 글이 207개 있는데도 게시판이 텅 빈 것처럼 읽힙니다
- **검색 폼은 별도 컴포넌트(`SearchForm`)로 두고 `key`에 주소의 검색 조건을 겁니다.** 이유가 두 가지입니다. 타이핑 중인 값을 주소에 바로 반영하면 글자 하나마다 DB를 조회하고 방문 기록이 그만큼 쌓여 뒤로가기를 스무 번 눌러야 합니다. 그렇다고 입력값을 컴포넌트 안에만 두면, `useState`의 초기값은 처음 나타날 때 한 번만 쓰이므로 뒤로가기나 "검색 해제"로 **주소가 먼저 바뀌었을 때** 입력칸에 옛 검색어가 남습니다. `key`가 바뀌면 React가 컴포넌트를 새로 만들기 때문에 초기값이 다시 들어갑니다. `PostEdit.jsx`가 수정 폼을 떼어둔 것과 같은 이유입니다
- **`alert` 대신 공용 `Modal` 컴포넌트.** ESC로 닫히고, 안내용(확인 하나)과 확인용(취소/확인 둘) 두 형태를 씁니다
- **되돌아갈 곳을 못 박습니다.** `navigate(-1)`은 브라우저 뒤로가기와 같아서, 주소를 직접 쳐서 들어온 경우 앞 기록이 다른 사이트라 거기로 나가버립니다
- **인증 에러 한글화** (`src/authErrors.js`), SEO 메타·OG 태그, 폼 입력칸 `aria-label`, `robots.txt`

---

## 9. 트러블슈팅 기록

### 9-1. 작성자 이메일이 노출되고 있었음 · `0b46ca9`

조회 정책이 "누구나 허용"이라, `writer`에 이메일을 저장하면 **로그인하지 않은 사람도 개발자도구 네트워크 탭에서 가입자 이메일을 그대로** 볼 수 있었습니다.

```jsx
// Before
.insert([{ title, content, writer: user.email, user_id: user.id }])
// After — 닉네임을 저장 (이메일은 애초에 DB에 넣지 않음)
.insert([{ title, content, writer: nickname, user_id: user.id }])
```

화면에서 가리는 것으로는 막을 수 없습니다. **DB에 민감정보를 넣지 않는 방식**으로 해결했고, 이때 `profiles` 표와 트리거를 도입했습니다.

### 9-2. 수정·삭제 실패를 성공으로 안내 · `fde8bcd`, `97804df`

`.select()` 없이 update/delete를 하면 RLS가 막아도 `error`가 `null`이라 "수정되었습니다"로 안내했습니다. 또 주소창으로 남의 글 수정 폼에 진입할 수 있었습니다. → 6-2, 6-3으로 해결.

### 9-3. 로그인만 하면 도배 가능 · 화면 처리 `94fdb7d`

등록 정책이 `auth.uid() = user_id`만 검사했습니다. INSERT 정책에 "최근 1시간 내 10개 미만" 조건을 추가해 **DB 차원에서** 막고, 화면은 `42501`을 알아보고 안내 모달을 띄웁니다.

### 9-4. 없는 페이지·삭제 후 404 · `290b4eb`, `862f633`

주소로 없는 페이지 번호에 접근하면 빈 목록이 나왔고, 삭제로 페이지가 줄면 사라진 페이지로 돌아가 404가 떴습니다. → 8번 항목으로 해결.

### 9-5. 전체 글을 한 번에 받아오던 구조 · (아직 커밋 전)

리뷰에서 "왜 한 번에 다 받아오냐"는 지적을 받아 서버 페이징으로 바꿨습니다. → 4번 참고.

> `supabase/` 폴더는 `.gitignore`에 있어 git에 올라가지 않습니다. 9-3의 Before는 커밋 diff가 아니라 개념적 이전 상태입니다.

---

## 10. 폴더 구조

```
src/
├─ App.jsx            # 라우팅 + 헤더 (글 데이터는 들고 있지 않음)
├─ main.jsx           # 진입점 (BrowserRouter / AuthProvider)
├─ AuthContext.jsx    # 로그인 사용자·닉네임 Context
├─ authErrors.js      # Supabase 인증 에러 → 한글 메시지
├─ format.js          # 작성자·날짜 표시 형식
├─ search.js          # 검색 대상 목록 + 검색어 이스케이프
├─ supabase.js        # Supabase 클라이언트
├─ components/
│  ├─ Modal.jsx       # 안내용 / 확인용 모달
│  └─ Comments.jsx    # 댓글 목록 + 입력칸
└─ pages/
   ├─ PostList.jsx    # 목록 + 서버 페이징 + 검색
   ├─ PostWrite.jsx   # 글쓰기
   ├─ PostDetail.jsx  # 상세 + 삭제
   ├─ PostEdit.jsx    # 수정
   ├─ Login.jsx / Signup.jsx
   ├─ ComingSoon.jsx  # 아직 안 만든 메뉴
   └─ NotFound.jsx    # 404

supabase/             # git에 올리지 않음 (대시보드 설정의 기록본)
├─ policy.sql         # posts RLS 정책
├─ profiles.sql       # profiles 표 + 닉네임 트리거
└─ comments.sql       # comments 표 + RLS 정책
```

---

## 11. 스택 버전

| 구분 | 패키지 | 버전 |
|---|---|---|
| 핵심 | react / react-dom | ^19.2.7 |
| 라우팅 | react-router-dom | ^7.18.1 |
| 백엔드(BaaS) | @supabase/supabase-js | ^2.110.7 |
| 빌드 | vite | ^8.1.1 |
| 빌드 | @vitejs/plugin-react | ^6.0.3 |
| 린트 | eslint / @eslint/js | ^10.6.0 / ^10.0.1 |
| 린트 | eslint-plugin-react-hooks | ^7.1.1 |
| 린트 | eslint-plugin-react-refresh | ^0.5.3 |

### React 19 / React Router 7의 신기능은 쓰지 않았습니다

최신 버전이 설치돼 있지만, 두 버전이 새로 내놓은 기능은 코드베이스에서 사용하지 않았습니다. 없는 것을 있다고 하지 않기 위해 사실대로 적어둡니다.

| 기능 | 이 프로젝트의 방식 |
|---|---|
| `use()` | Context는 `useContext`로 소비 |
| Actions / `<form action>` | `onSubmit` + `event.preventDefault()` |
| `useActionState` / `useFormStatus` | `useState`로 폼 상태와 버튼 잠금 직접 관리 |
| `useOptimistic` / `useTransition` | 미사용 |
| `createBrowserRouter` / `RouterProvider` | `<BrowserRouter>` + `<Routes>` / `<Route>` |
| `loader` / `useLoaderData` / `action` | `useEffect` + `supabase` 직접 호출 |

실제로 쓴 Router API: `BrowserRouter`, `Routes`, `Route`, `Link`, `NavLink`, `Navigate`, `useNavigate`, `useParams`, `useSearchParams`

> "React 19 / Router 7을 **썼다**"가 아니라 "최신 버전 위에서 **표준 훅 방식으로 구현했다**"가 정확한 표현입니다. 신기능 미사용은 단점이 아니라, 학습·이해 목적상 명시적이고 읽기 쉬운 코드를 택한 결정입니다.

---

## 12. 실행 방법

```bash
npm install
cp .env.example .env     # VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY 채우기
npm run dev              # 개발 서버
npm run build            # 프로덕션 빌드
npm run lint             # ESLint 검사
```

Supabase를 새로 붙이는 경우 `supabase/profiles.sql`을 대시보드 SQL Editor에서 **【1】【2】【3】 구간을 하나씩 따로** 실행해야 합니다. 전체를 한 번에 붙여 넣으면 함수 본문의 세미콜론을 문장 끝으로 잘못 읽어 `unterminated dollar-quoted string` 에러가 납니다.

`supabase/comments.sql`은 안에 함수가 없어서 **통째로 한 번에** 실행해도 됩니다.
